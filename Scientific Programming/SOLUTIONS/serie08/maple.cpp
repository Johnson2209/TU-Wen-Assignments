/*Write a class Matrix to save n-by-n square matrices. The class contains the data members
n (int) for the dimension, a dynamical vector coeff (double*), and a type type (char). The type allows
to distinguish between fully populated matrices (type 'F'), lower triangular matrices (type 'L'), and
upper triangular matrices (type 'U'). A lower triangular matrix L and an upper triangular matrix U*/
#include <iostream>
using namespace std;

class Matric{
  n int;

public:
  void coeff(double *);
  void type(char)
}type 'F', type 'L', type 'U';
